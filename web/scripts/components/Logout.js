import React from 'react';

const Logout = React.createClass({
    render:function runLogoutRender(){
        return(
            <div>Hello from Logout</div>
        );
     }
});

export default Logout;